(function() {
  if (/*@cc_on!@*/0) { // execute only in Internet Explorer
    var elements = [
      "dialog"//,
    ],
    i = elements.length;
    
    while (i--) {
      document.createElement(elements[i]);
    }
  }
})();
